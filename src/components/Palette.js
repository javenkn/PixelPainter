import React from 'react';
import Pixel from './Pixel';

export default class Palette extends React.Component {
  render () {
    return (
      <div id='palette'>
        <Pixel />
      </div>
    );
  }
}